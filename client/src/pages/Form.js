import React from 'react';
import ActivityForm from '../components/Form';
import Nav from '../components/Nav';

const Form = () => {
    return (
        <main>
            <Nav />
            <ActivityForm />
        </main>
    );
};

export default Form;
