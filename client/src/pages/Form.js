import React from 'react';
import ActivityForm from '../components/Form';

const Form = () => {
    return (
        <main>
            <ActivityForm />
        </main>
    );
};

export default Form;
